jobs:
  - name: selenium-manifest
    type: manifest
    steps:
     - IN: selenium-img-one
     - IN: selenium-img-two
     - TASK: managed

  - name: selenium-deploy
    type: deploy
    steps:
      - IN: selenium-manifest
      - IN: docker-opts
      - IN: selenium
      - TASK: managed
        deployMethod: replace
