jobs:
  - name: selenium-manifest
    type: manifest
    steps:
     - IN: selenium-conf_master
     - TASK: managed

  - name: selenium-deploy
    type: deploy
    steps:
      - IN: selenium-manifest
      - IN: docker-opts
      - IN: selenium
      - TASK: managed
        deployMethod: replace
