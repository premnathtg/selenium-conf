jobs:
  - name: selenium-manifest
    type: manifest
    steps:
     - IN: selenium-pl
     - TASK: managed

  - name: selenium-pl
    type: deploy
    steps:
      - IN: selenium-manifest
      - IN: docker-opts
      - IN: selenium
      - TASK: managed
        deployMethod: upgrade
